---
title: Fehlerbehebung
description: Haeufige Probleme und schnelle Loesungen.
---

Wenn ia-bot-ahmed Probleme macht, starte mit Logs und lokal gespeicherten Daten auf der Festplatte.

---

## Logs

Logdateien werden hier gespeichert:

- **macOS/Linux**: `~/.local/share/ia-bot-ahmed/log/`
- **Windows**: Druecke `WIN+R` und fuege ein: `%USERPROFILE%\.local\share\ia-bot-ahmed\log`

Dateinamen enthalten Zeitstempel (z. B. `2025-01-09T123456.log`) und es bleiben die letzten 10 Logs erhalten.

Mit `--log-level` bekommst du detailliertere Diagnoseinfos, z. B. `ia-bot-ahmed --log-level DEBUG`.

---

## Speicher

ia-bot-ahmed speichert Sitzungs- und App-Daten auf der Festplatte unter:

- **macOS/Linux**: `~/.local/share/ia-bot-ahmed/`
- **Windows**: Druecke `WIN+R` und fuege ein: `%USERPROFILE%\.local\share\ia-bot-ahmed`

Dieses Verzeichnis enthaelt:

- `auth.json` - Authentifizierungsdaten wie API-Keys und OAuth-Tokens
- `log/` - Anwendungslogs
- `project/` - Projektspezifische Daten wie Sitzungen und Nachrichten
  - In Git-Repositories unter `./<project-slug>/storage/`
  - Ohne Git unter `./global/storage/`

---

## Desktop-App

ia-bot-ahmed Desktop startet im Hintergrund einen lokalen ia-bot-ahmed-Server (`ia-bot-ahmed-cli`-Sidecar).
Viele Probleme kommen von fehlerhaften Plugins, kaputtem Cache oder falschen Server-Einstellungen.

### Schnellchecks

- App komplett beenden und neu starten
- Bei Fehlerbildschirm **Restart** klicken und Details kopieren
- Nur macOS: `ia-bot-ahmed`-Menue -> **Reload Webview** (hilft bei leerer/eingefrorener UI)

---

### Plugins deaktivieren

Wenn die Desktop-App beim Start abstuerzt, haengt oder sich seltsam verhaelt, deaktiviere zunaechst Plugins.

#### Globale Konfiguration prüfen

Oeffne deine globale Konfigurationsdatei und suche nach dem `plugin`-Schluessel.

- **macOS/Linux**: `~/.config/ia-bot-ahmed/ia-bot-ahmed.jsonc` (oder `~/.config/ia-bot-ahmed/ia-bot-ahmed.json`)
- **macOS/Linux** (aeltere Installationen): `~/.local/share/ia-bot-ahmed/ia-bot-ahmed.jsonc`
- **Windows**: Druecke `WIN+R` und fuege ein: `%USERPROFILE%\.config\ia-bot-ahmed\ia-bot-ahmed.jsonc`

Wenn du Plugins konfiguriert hast, deaktiviere sie voruebergehend, indem du den Schluessel entfernst oder auf ein leeres Array setzt:

```jsonc
{
  "$schema": "https://ia-bot-ahmed.app/config.json",
  "plugin": [],
}
```

#### Plugin-Verzeichnisse prüfen

ia-bot-ahmed kann auch lokale Plugins von der Festplatte laden. Verschiebe diese voruebergehend (oder benenne den Ordner um) und starte die Desktop-App neu:

- **Globale Plugins**
  - **macOS/Linux**: `~/.config/ia-bot-ahmed/plugins/`
  - **Windows**: Druecke `WIN+R` und fuege ein: `%USERPROFILE%\.config\ia-bot-ahmed\plugins`
- **Projekt-Plugins** (nur bei projektspezifischer Konfig)
  - `<your-project>/.ia-bot-ahmed/plugins/`

Wenn die App wieder funktioniert, aktiviere Plugins nacheinander, um den Verursacher zu finden.

---

### Cache leeren

Wenn das Deaktivieren von Plugins nicht hilft (oder eine Plugin-Installation haengt), leere den Cache, damit ia-bot-ahmed ihn neu aufbauen kann.

1. Beende ia-bot-ahmed Desktop komplett.
2. Loesche das Cache-Verzeichnis:

- **macOS**: Finder -> `Cmd+Shift+G` -> einfuegen: `~/.cache/ia-bot-ahmed`
- **Linux**: loesche `~/.cache/ia-bot-ahmed` (oder fuehre aus: `rm -rf ~/.cache/ia-bot-ahmed`)
- **Windows**: Druecke `WIN+R` und fuege ein: `%USERPROFILE%\.cache\ia-bot-ahmed`

3. Starte ia-bot-ahmed Desktop neu.

---

### Server-Verbindungsprobleme beheben

ia-bot-ahmed Desktop kann entweder einen eigenen lokalen Server starten (Standard) oder sich mit einer konfigurierten Server-URL verbinden.

Wenn du einen **"Connection Failed"**-Dialog siehst (oder die App beim Splash-Screen haengen bleibt), pruefe auf eine benutzerdefinierte Server-URL.

#### Desktop-Standard-Server-URL löschen

Klicke im Startbildschirm auf den Servernamen (mit dem Statuspunkt), um die Serverauswahl zu oeffnen. Klicke im Bereich **Default server** auf **Clear**.

#### `server.port` / `server.hostname` aus Konfiguration entfernen

Wenn deine `ia-bot-ahmed.json(c)` einen `server`-Abschnitt enthaelt, entferne ihn voruebergehend und starte die Desktop-App neu.

#### Umgebungsvariablen prüfen

Wenn du `IA_BOT_AHMED_PORT` in deiner Umgebung gesetzt hast, versucht die Desktop-App diesen Port fuer den lokalen Server zu nutzen.

- Setze `IA_BOT_AHMED_PORT` zurueck (oder waehle einen freien Port) und starte neu.

---

### Linux: Wayland / X11-Probleme

Unter Linux koennen manche Wayland-Setups leere Fenster oder Compositor-Fehler verursachen.

- Wenn du Wayland nutzt und die App leer ist/abstuerzt, versuche den Start mit `OC_ALLOW_WAYLAND=1`.
- Wenn das es verschlimmert, entferne es und versuche den Start in einer X11-Session.

---

### Windows: WebView2-Laufzeit

Unter Windows benoetigt ia-bot-ahmed Desktop die Microsoft Edge **WebView2 Runtime**. Wenn die App ein leeres Fenster zeigt oder nicht startet, installiere/aktualisiere WebView2 und versuche es erneut.

---

### Windows: Allgemeine Performance-Probleme

Wenn du langsame Performance, Dateizugriffsprobleme oder Terminal-Probleme unter Windows hast, versuche [WSL (Windows Subsystem for Linux)](/docs/windows-wsl). WSL bietet eine Linux-Umgebung, die nahtloser mit ia-bot-ahmed-Features funktioniert.

---

### Benachrichtigungen werden nicht angezeigt

ia-bot-ahmed Desktop zeigt Systembenachrichtigungen nur wenn:

- Benachrichtigungen fuer ia-bot-ahmed in den OS-Einstellungen aktiviert sind, und
- das App-Fenster nicht fokussiert ist.

---

### Desktop-App-Speicher zurücksetzen (letzter Ausweg)

Wenn die App nicht startet und du Einstellungen nicht in der UI loeschen kannst, setze den gespeicherten Zustand der Desktop-App zurueck.

1. Beende ia-bot-ahmed Desktop.
2. Finde und loesche diese Dateien (im App-Data-Verzeichnis von ia-bot-ahmed Desktop):

- `ia-bot-ahmed.settings.dat` (desktop default server URL)
- `ia-bot-ahmed.global.dat` und `ia-bot-ahmed.workspace.*.dat` (UI state like recent servers/projects)

So findest du das Verzeichnis schnell:

- **macOS**: Finder -> `Cmd+Shift+G` -> `~/Library/Application Support` (dann suche nach den Dateinamen oben)
- **Linux**: suche unter `~/.local/share` nach den Dateinamen oben
- **Windows**: Druecke `WIN+R` -> `%APPDATA%` (dann suche nach den Dateinamen oben)

---

## Hilfe bekommen

Wenn du Probleme mit ia-bot-ahmed hast:

1. **Probleme auf GitHub melden**

   Bugs und Feature-Wuensche meldest du am besten im GitHub-Repository:

   [**github.com/anomalyco/ia-bot-ahmed/issues**](https://github.com/anomalyco/ia-bot-ahmed/issues)

   Suche vor dem Erstellen nach bestehenden Issues, damit Duplikate vermieden werden.

2. **Unserem Discord beitreten**

   Fuer schnelle Hilfe und Austausch in der Community:

   [**ia-bot-ahmed.app/discord**](https://ia-bot-ahmed.app/discord)

---

## Haeufige Probleme

Hier sind typische Fehlerbilder und wie du sie loest.

---

### ia-bot-ahmed startet nicht

1. Pruefe die Logs auf Fehlermeldungen
2. Versuche den Start mit `--print-logs`, um Ausgaben im Terminal zu sehen
3. Stelle sicher, dass du die neueste Version hast: `ia-bot-ahmed upgrade`

---

### Authentifizierungsprobleme

1. Versuche erneute Authentifizierung mit `/connect` in der TUI
2. Pruefe, ob deine API-Keys gueltig sind
3. Stelle sicher, dass dein Netzwerk Verbindungen zur Provider-API erlaubt

---

### Modell nicht verfügbar

1. Pruefe, ob du dich beim Provider authentifiziert hast
2. Verifiziere, dass der Modellname in deiner Config korrekt ist
3. Manche Modelle erfordern speziellen Zugriff oder Abonnements

Wenn du `ProviderModelNotFoundError` erhaeltst, referenzierst du ein Modell wahrscheinlich falsch.
Modelle sollten so referenziert werden: `<providerId>/<modelId>`

Beispiele:

- `openai/gpt-4.1`
- `openrouter/google/gemini-2.5-flash`
- `ia-bot-ahmed/kimi-k2`

Um zu sehen, auf welche Modelle du Zugriff hast, fuehre `ia-bot-ahmed models` aus.

---

### ProviderInitError

Wenn du einen ProviderInitError erhaeltst, hast du wahrscheinlich eine ungueltige oder korrupte Konfiguration.

Zur Loesung:

1. Pruefe zuerst, ob dein Provider korrekt eingerichtet ist, gemaess dem [Provider-Guide](/docs/providers)
2. Wenn das Problem besteht, versuche deine gespeicherte Konfiguration zu loeschen:

   ```bash
   rm -rf ~/.local/share/ia-bot-ahmed
   ```

   Unter Windows druecke `WIN+R` und loesche: `%USERPROFILE%\.local\share\ia-bot-ahmed`

3. Authentifiziere dich erneut beim Provider mit dem `/connect`-Befehl in der TUI.

---

### AI_APICallError und Provider-Paket-Probleme

Wenn du API-Call-Fehler erhaeltst, kann das an veralteten Provider-Paketen liegen. ia-bot-ahmed installiert Provider-Pakete (OpenAI, Anthropic, Google, etc.) dynamisch bei Bedarf und cached sie lokal.

Um Provider-Paket-Probleme zu loesen:

1. Leere den Provider-Paket-Cache:

   ```bash
   rm -rf ~/.cache/ia-bot-ahmed
   ```

   Unter Windows druecke `WIN+R` und loesche: `%USERPROFILE%\.cache\ia-bot-ahmed`

2. Starte ia-bot-ahmed neu, um die neuesten Provider-Pakete zu installieren

Dies zwingt ia-bot-ahmed, die neuesten Versionen der Provider-Pakete herunterzuladen, was oft Kompatibilitaetsprobleme mit Modellparametern und API-Aenderungen loest.

---

### Copy/Paste funktioniert nicht unter Linux

Linux-Nutzer muessen eines der folgenden Clipboard-Utilities installiert haben, damit Copy/Paste funktioniert:

**Fuer X11-Systeme:**

```bash
apt install -y xclip
# or
apt install -y xsel
```

**Fuer Wayland-Systeme:**

```bash
apt install -y wl-clipboard
```

**Fuer Headless-Umgebungen:**

```bash
apt install -y xvfb
# and run:
Xvfb :99 -screen 0 1024x768x24 > /dev/null 2>&1 &
export DISPLAY=:99.0
```

ia-bot-ahmed erkennt, ob du Wayland nutzt und bevorzugt `wl-clipboard`, sonst versucht es `xclip` und `xsel` (in dieser Reihenfolge).
