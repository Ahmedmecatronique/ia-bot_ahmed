---
title: CLI
description: Opzioni e comandi della CLI di ia-bot-ahmed.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

La CLI di ia-bot-ahmed, per impostazione predefinita, avvia la [TUI](/docs/tui) quando viene eseguita senza argomenti.

```bash
ia-bot-ahmed
```

Ma accetta anche i comandi documentati in questa pagina. Questo ti permette di interagire con ia-bot-ahmed in modo programmatico.

```bash
ia-bot-ahmed run "Explain how closures work in JavaScript"
```

---

### tui

Avvia l'interfaccia testuale di ia-bot-ahmed (interfaccia utente da terminale).

```bash
ia-bot-ahmed [project]
```

#### Flag

| Flag                                     | Breve | Descrizione                                                              |
| ---------------------------------------- | ----- | ------------------------------------------------------------------------ |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continua l'ultima sessione                                               |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID sessione da continuare                                                |
| <nobr><code>{"--fork"}</code></nobr>     |       | Duplica la sessione quando continui (usa con `--continue` o `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |       | Prompt da usare                                                          |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Modello nel formato provider/model                                       |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agente da usare                                                          |
| <nobr><code>{"--port"}</code></nobr>     |       | Porta su cui mettersi in ascolto                                         |
| <nobr><code>{"--hostname"}</code></nobr> |       | Hostname su cui mettersi in ascolto                                      |

---

## Comandi

La CLI di ia-bot-ahmed include anche i seguenti comandi.

---

### agent

Gestisci gli agenti per ia-bot-ahmed.

```bash
ia-bot-ahmed agent [command]
```

---

### attach

Collega un terminale a un backend server di ia-bot-ahmed già in esecuzione avviato tramite i comandi `serve` o `web`.

```bash
ia-bot-ahmed attach [url]
```

Questo consente di usare la TUI con un backend ia-bot-ahmed remoto. Per esempio:

```bash
# Start the backend server for web/mobile access
ia-bot-ahmed web --port 4096 --hostname 0.0.0.0

# In un altro terminale, collega la TUI al backend in esecuzione
ia-bot-ahmed attach http://10.20.30.40:4096
```

#### Flag

| Flag                                     | Breve | Descrizione                                                                                     |
| ---------------------------------------- | ----- | ----------------------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |       | Working directory in cui avviare la TUI                                                         |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continua l'ultima sessione                                                                      |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID sessione da continuare                                                                       |
| <nobr><code>{"--fork"}</code></nobr>     |       | Crea un fork della sessione durante la continuazione (usa con `--continue` o `--session`)       |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Password per l'autenticazione di base (predefinita: `IA_BOT_AHMED_SERVER_PASSWORD`)                 |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Nome utente per l'autenticazione di base (predefinito: `IA_BOT_AHMED_SERVER_USERNAME` o `ia-bot-ahmed`) |

---

#### create

Crea un nuovo agente con configurazione personalizzata.

```bash
ia-bot-ahmed agent create
```

Questo comando ti guida nella creazione di un nuovo agente con un system prompt personalizzato e configurazione degli strumenti.

---

#### list

Elenca tutti gli agenti disponibili.

```bash
ia-bot-ahmed agent list
```

---

### auth

Comando per gestire le credenziali e il login dei provider.

```bash
ia-bot-ahmed auth [command]
```

---

#### login

ia-bot-ahmed si basa sull'elenco provider di [Models.dev](https://models.dev), quindi puoi usare `ia-bot-ahmed auth login` per configurare le API key per qualunque provider tu voglia usare. Le credenziali vengono salvate in `~/.local/share/ia-bot-ahmed/auth.json`.

```bash
ia-bot-ahmed auth login
```

Quando ia-bot-ahmed si avvia, carica i provider dal file delle credenziali e, se presenti, anche eventuali key definite nell'ambiente o in un file `.env` nel progetto.

---

#### list

Elenca tutti i provider autenticati come salvati nel file delle credenziali.

```bash
ia-bot-ahmed auth list
```

Oppure la versione corta.

```bash
ia-bot-ahmed auth ls
```

---

#### logout

Esegue il logout da un provider rimuovendolo dal file delle credenziali.

```bash
ia-bot-ahmed auth logout
```

---

### github

Gestisci l'agente GitHub per l'automazione dei repository.

```bash
ia-bot-ahmed github [command]
```

---

#### install

Installa l'agente GitHub nel tuo repository.

```bash
ia-bot-ahmed github install
```

Questo configura il workflow GitHub Actions necessario e ti guida nel processo di configurazione. [Scopri di più](/docs/github).

---

#### run

Esegui l'agente GitHub. Tipicamente usato in GitHub Actions.

```bash
ia-bot-ahmed github run
```

##### Flag

| Flag                                  | Descrizione                                  |
| ------------------------------------- | -------------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | Evento GitHub mock per cui eseguire l'agente |
| <nobr><code>{"--token"}</code></nobr> | GitHub personal access token                 |

---

### mcp

Gestisci i server Model Context Protocol.

```bash
ia-bot-ahmed mcp [command]
```

---

#### add

Aggiungi un server MCP alla tua configurazione.

```bash
ia-bot-ahmed mcp add
```

Questo comando ti guida nell'aggiunta di un server MCP locale o remoto.

---

#### list

Elenca tutti i server MCP configurati e il loro stato di connessione.

```bash
ia-bot-ahmed mcp list
```

Oppure la versione corta.

```bash
ia-bot-ahmed mcp ls
```

---

#### auth

Autentica con un server MCP con OAuth abilitato.

```bash
ia-bot-ahmed mcp auth [name]
```

Se non fornisci un nome server, ti verrà chiesto di selezionare tra i server OAuth-capable disponibili.

Puoi anche elencare i server OAuth-capable e il loro stato di autenticazione.

```bash
ia-bot-ahmed mcp auth list
```

Oppure la versione corta.

```bash
ia-bot-ahmed mcp auth ls
```

---

#### logout

Rimuovi le credenziali OAuth per un server MCP.

```bash
ia-bot-ahmed mcp logout [name]
```

---

#### debug

Esegui debug di problemi di connessione OAuth per un server MCP.

```bash
ia-bot-ahmed mcp debug <name>
```

---

### models

Elenca tutti i modelli disponibili dai provider configurati.

```bash
ia-bot-ahmed models [provider]
```

Questo comando mostra tutti i modelli disponibili tra i provider configurati nel formato `provider/model`.

È utile per capire l'esatto nome del modello da usare nella [config](/docs/config/).

Puoi anche passare opzionalmente un ID provider per filtrare i modelli a quel provider.

```bash
ia-bot-ahmed models anthropic
```

#### Flag

| Flag                                    | Descrizione                                        |
| --------------------------------------- | -------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Aggiorna la cache modelli da models.dev            |
| <nobr><code>{"--verbose"}</code></nobr> | Output più verboso (include metadati come i costi) |

Usa `--refresh` per aggiornare l'elenco modelli in cache. È utile quando nuovi modelli vengono aggiunti a un provider e vuoi vederli in ia-bot-ahmed.

```bash
ia-bot-ahmed models --refresh
```

---

### run

Esegui ia-bot-ahmed in modalità non interattiva passando un prompt direttamente.

```bash
ia-bot-ahmed run [message..]
```

È utile per scripting, automazione o quando vuoi una risposta rapida senza avviare la TUI completa. Per esempio:

```bash "ia-bot-ahmed run"
ia-bot-ahmed run Explain the use of context in Go
```

Puoi anche collegarti a una istanza `ia-bot-ahmed serve` già in esecuzione per evitare i cold boot dei server MCP ad ogni esecuzione:

```bash
# Avvia un server headless in un terminale
ia-bot-ahmed serve

# In un altro terminale, esegui comandi che si collegano ad esso
ia-bot-ahmed run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flag

| Flag                                     | Breve | Descrizione                                                                                     |
| ---------------------------------------- | ----- | ----------------------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |       | Il comando da eseguire; usa message per gli argomenti                                           |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continua l'ultima sessione                                                                      |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID sessione da continuare                                                                       |
| <nobr><code>{"--fork"}</code></nobr>     |       | Duplica la sessione quando continui (usa con `--continue` o `--session`)                        |
| <nobr><code>{"--share"}</code></nobr>    |       | Condividi la sessione                                                                           |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Modello nel formato provider/model                                                              |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agente da usare                                                                                 |
| <nobr><code>{"--file"}</code></nobr>     | `-f`  | File da allegare al messaggio                                                                   |
| <nobr><code>{"--format"}</code></nobr>   |       | Formato: default (formattato) o json (eventi JSON grezzi)                                       |
| <nobr><code>{"--title"}</code></nobr>    |       | Titolo sessione (usa prompt troncato se non viene fornito un valore)                            |
| <nobr><code>{"--attach"}</code></nobr>   |       | Attach a un server ia-bot-ahmed in esecuzione (es. http://localhost:4096)                           |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Password per l'autenticazione di base (predefinita: `IA_BOT_AHMED_SERVER_PASSWORD`)                 |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Nome utente per l'autenticazione di base (predefinito: `IA_BOT_AHMED_SERVER_USERNAME` o `ia-bot-ahmed`) |
| <nobr><code>{"--dir"}</code></nobr>      |       | Directory di esecuzione, o percorso sul server remoto durante il collegamento                   |
| <nobr><code>{"--variant"}</code></nobr>  |       | Variante del modello (sforzo di ragionamento specifico del provider)                            |
| <nobr><code>{"--thinking"}</code></nobr> |       | Mostra blocchi di pensiero                                                                      |
| <nobr><code>{"--port"}</code></nobr>     |       | Porta per il server locale (di default una porta casuale)                                       |

---

### serve

Avvia un server ia-bot-ahmed headless per accesso via API. Vedi le [server docs](/docs/server) per l'interfaccia HTTP completa.

```bash
ia-bot-ahmed serve
```

Avvia un server HTTP che espone accesso API alle funzionalità di ia-bot-ahmed senza la TUI. Imposta `IA_BOT_AHMED_SERVER_PASSWORD` per abilitare HTTP basic auth (username di default `ia-bot-ahmed`).

#### Flag

| Flag                                     | Descrizione                                    |
| ---------------------------------------- | ---------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Porta su cui mettersi in ascolto               |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname su cui mettersi in ascolto            |
| <nobr><code>{"--mdns"}</code></nobr>     | Abilita discovery mDNS                         |
| <nobr><code>{"--cors"}</code></nobr>     | Origin browser addizionali per consentire CORS |

---

### session

Gestisci le sessioni ia-bot-ahmed.

```bash
ia-bot-ahmed session [command]
```

---

#### list

Elenca tutte le sessioni ia-bot-ahmed.

```bash
ia-bot-ahmed session list
```

##### Flag

| Flag                                      | Breve | Descrizione                          |
| ----------------------------------------- | ----- | ------------------------------------ |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`  | Limita alle N sessioni più recenti   |
| <nobr><code>{"--format"}</code></nobr>    |       | Formato output: table o json (table) |

---

### stats

Mostra statistiche di utilizzo token e costo per le sessioni ia-bot-ahmed.

```bash
ia-bot-ahmed stats
```

#### Flag

| Flag                                    | Descrizione                                                                           |
| --------------------------------------- | ------------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Mostra statistiche per gli ultimi N giorni (all time)                                 |
| <nobr><code>{"--tools"}</code></nobr>   | Numero di strumenti da mostrare (all)                                                 |
| <nobr><code>{"--models"}</code></nobr>  | Mostra breakdown di utilizzo modelli (nascosto di default). Passa un numero per top N |
| <nobr><code>{"--project"}</code></nobr> | Filtra per progetto (tutti i progetti; stringa vuota: progetto corrente)              |

---

### export

Esporta i dati di sessione come JSON.

```bash
ia-bot-ahmed export [sessionID]
```

Se non fornisci un ID sessione, ti verrà chiesto di selezionare tra le sessioni disponibili.

---

### import

Importa i dati di sessione da un file JSON o da un URL di condivisione ia-bot-ahmed.

```bash
ia-bot-ahmed import <file>
```

Puoi importare da un file locale o da un URL di condivisione ia-bot-ahmed.

```bash
ia-bot-ahmed import session.json
ia-bot-ahmed import https://opncd.ai/s/abc123
```

---

### web

Avvia un server ia-bot-ahmed headless con interfaccia web.

```bash
ia-bot-ahmed web
```

Avvia un server HTTP e apre un browser per accedere a ia-bot-ahmed tramite interfaccia web. Imposta `IA_BOT_AHMED_SERVER_PASSWORD` per abilitare HTTP basic auth (username di default `ia-bot-ahmed`).

#### Flag

| Flag                                     | Descrizione                                    |
| ---------------------------------------- | ---------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Porta su cui mettersi in ascolto               |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname su cui mettersi in ascolto            |
| <nobr><code>{"--mdns"}</code></nobr>     | Abilita discovery mDNS                         |
| <nobr><code>{"--cors"}</code></nobr>     | Origin browser addizionali per consentire CORS |

---

### acp

Avvia un server ACP (Agent Client Protocol).

```bash
ia-bot-ahmed acp
```

Questo comando avvia un server ACP che comunica via stdin/stdout usando nd-JSON.

#### Flag

| Flag                                     | Descrizione                         |
| ---------------------------------------- | ----------------------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Directory di lavoro                 |
| <nobr><code>{"--port"}</code></nobr>     | Porta su cui mettersi in ascolto    |
| <nobr><code>{"--hostname"}</code></nobr> | Hostname su cui mettersi in ascolto |

---

### uninstall

Disinstalla ia-bot-ahmed e rimuove tutti i file correlati.

```bash
ia-bot-ahmed uninstall
```

#### Flag

| Flag                                        | Breve | Descrizione                                  |
| ------------------------------------------- | ----- | -------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`  | Mantieni i file di configurazione            |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`  | Mantieni dati di sessione e snapshot         |
| <nobr><code>{"--dry-run"}</code></nobr>     |       | Mostra cosa verrebbe rimosso senza rimuovere |
| <nobr><code>{"--force"}</code></nobr>       | `-f`  | Salta le richieste di conferma               |

---

### upgrade

Aggiorna ia-bot-ahmed all'ultima versione o a una versione specifica.

```bash
ia-bot-ahmed upgrade [target]
```

Per aggiornare all'ultima versione:

```bash
ia-bot-ahmed upgrade
```

Per aggiornare a una versione specifica:

```bash
ia-bot-ahmed upgrade v0.1.48
```

#### Flag

| Flag                                   | Breve | Descrizione                                               |
| -------------------------------------- | ----- | --------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`  | Metodo di installazione usato: curl, npm, pnpm, bun, brew |

---

## Flag globali

La CLI di ia-bot-ahmed accetta i seguenti flag globali.

| Flag                                       | Breve | Descrizione                            |
| ------------------------------------------ | ----- | -------------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h`  | Mostra l'help                          |
| <nobr><code>{"--version"}</code></nobr>    | `-v`  | Stampa il numero di versione           |
| <nobr><code>{"--print-logs"}</code></nobr> |       | Stampa i log su stderr                 |
| <nobr><code>{"--log-level"}</code></nobr>  |       | Livello log (DEBUG, INFO, WARN, ERROR) |

---

## Variabili d'ambiente

ia-bot-ahmed può essere configurato tramite variabili d'ambiente.

| Variabile                             | Tipo    | Descrizione                                                 |
| ------------------------------------- | ------- | ----------------------------------------------------------- |
| `IA_BOT_AHMED_AUTO_SHARE`                 | boolean | Condivide automaticamente le sessioni                       |
| `IA_BOT_AHMED_GIT_BASH_PATH`              | string  | Percorso all'eseguibile Git Bash su Windows                 |
| `IA_BOT_AHMED_CONFIG`                     | string  | Percorso al file di configurazione                          |
| `IA_BOT_AHMED_TUI_CONFIG`                 | string  | Percorso al file di configurazione TUI                      |
| `IA_BOT_AHMED_CONFIG_DIR`                 | string  | Percorso alla directory di configurazione                   |
| `IA_BOT_AHMED_CONFIG_CONTENT`             | string  | Contenuto JSON di config inline                             |
| `IA_BOT_AHMED_DISABLE_AUTOUPDATE`         | boolean | Disabilita i controlli automatici di aggiornamento          |
| `IA_BOT_AHMED_DISABLE_PRUNE`              | boolean | Disabilita la potatura dei dati vecchi                      |
| `IA_BOT_AHMED_DISABLE_TERMINAL_TITLE`     | boolean | Disabilita aggiornamenti automatici del titolo terminale    |
| `IA_BOT_AHMED_PERMISSION`                 | string  | Config permessi JSON inline                                 |
| `IA_BOT_AHMED_DISABLE_DEFAULT_PLUGINS`    | boolean | Disabilita i plugin di default                              |
| `IA_BOT_AHMED_DISABLE_LSP_DOWNLOAD`       | boolean | Disabilita download automatico dei server LSP               |
| `IA_BOT_AHMED_ENABLE_EXPERIMENTAL_MODELS` | boolean | Abilita modelli sperimentali                                |
| `IA_BOT_AHMED_DISABLE_AUTOCOMPACT`        | boolean | Disabilita compaction automatica del contesto               |
| `IA_BOT_AHMED_DISABLE_CLAUDE_CODE`        | boolean | Disabilita lettura da `.claude` (prompt + skill)            |
| `IA_BOT_AHMED_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disabilita lettura di `~/.claude/CLAUDE.md`                 |
| `IA_BOT_AHMED_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disabilita caricamento di `.claude/skills`                  |
| `IA_BOT_AHMED_DISABLE_MODELS_FETCH`       | boolean | Disabilita fetch dei modelli da fonti remote                |
| `IA_BOT_AHMED_FAKE_VCS`                   | string  | Provider VCS finto per scopi di test                        |
| `IA_BOT_AHMED_CLIENT`                     | string  | Identificatore client (default `cli`)                       |
| `IA_BOT_AHMED_ENABLE_EXA`                 | boolean | Abilita gli strumenti di web search Exa                     |
| `IA_BOT_AHMED_SERVER_PASSWORD`            | string  | Abilita basic auth per `serve`/`web`                        |
| `IA_BOT_AHMED_SERVER_USERNAME`            | string  | Sovrascrive lo username basic auth (default `ia-bot-ahmed`)     |
| `IA_BOT_AHMED_MODELS_URL`                 | string  | URL personalizzato per recuperare la configurazione modelli |

---

### Sperimentale

Queste variabili d'ambiente abilitano funzionalità sperimentali che potrebbero cambiare o essere rimosse.

| Variabile                                       | Tipo    | Descrizione                                             |
| ----------------------------------------------- | ------- | ------------------------------------------------------- |
| `IA_BOT_AHMED_EXPERIMENTAL`                         | boolean | Abilita funzionalità sperimentali sotto il flag globale |
| `IA_BOT_AHMED_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Abilita icon discovery                                  |
| `IA_BOT_AHMED_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disabilita copy on select nella TUI                     |
| `IA_BOT_AHMED_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Timeout di default per comandi bash in ms               |
| `IA_BOT_AHMED_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Massimo token di output per risposte LLM                |
| `IA_BOT_AHMED_EXPERIMENTAL_FILEWATCHER`             | boolean | Abilita file watcher per l'intera dir                   |
| `IA_BOT_AHMED_EXPERIMENTAL_OXFMT`                   | boolean | Abilita formatter oxfmt                                 |
| `IA_BOT_AHMED_EXPERIMENTAL_LSP_TOOL`                | boolean | Abilita strumento LSP sperimentale                      |
| `IA_BOT_AHMED_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disabilita file watcher                                 |
| `IA_BOT_AHMED_EXPERIMENTAL_EXA`                     | boolean | Abilita funzionalità Exa sperimentali                   |
| `IA_BOT_AHMED_EXPERIMENTAL_LSP_TY`                  | boolean | Abilita TY LSP per i file python                        |
| `IA_BOT_AHMED_EXPERIMENTAL_PLAN_MODE`               | boolean | Abilita plan mode                                       |
| `IA_BOT_AHMED_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Abilita task subagent in background                     |
| `IA_BOT_AHMED_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Abilita sistema eventi sperimentale                     |
| `IA_BOT_AHMED_EXPERIMENTAL_NATIVE_LLM`              | boolean | Abilita percorso nativo di richiesta LLM                |
| `IA_BOT_AHMED_EXPERIMENTAL_PARALLEL`                | boolean | Abilita esecuzione parallela della ricerca web          |
| `IA_BOT_AHMED_EXPERIMENTAL_SCOUT`                   | boolean | Abilita subagent Scout                                  |
| `IA_BOT_AHMED_EXPERIMENTAL_WORKSPACES`              | boolean | Abilita supporto workspace                              |
